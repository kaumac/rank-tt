import auth from './auth'
import firebaseApp, { db } from './config'
import pushDoc from './pushDoc'
import updateDoc from './updateDoc'

export { auth, db, pushDoc, updateDoc }

export default firebaseApp
